args:
  app_name:
    help: "Name of the app"
  gcp_region:
    help: "GCP Region"

filename_substitutions:
  __APP_NAME__: app_name
  
template_context:
  app_name: app_name
  gcp_region: gcp_region
